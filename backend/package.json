{
  "type": "module",
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "prisma generate",
    "dev": "nodemon server.js",
    "seed": "node prisma/seedDB.js",
    "clear": "node prisma/clearDB.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^5.17.0",
    "nodemon": "^3.0.1",
    "faker": "^4.1.0" // Remove if not used in the backend
  }
}
